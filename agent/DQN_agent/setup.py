from setuptools import setup, find_packages

setup(
    name='DQN_agent',
    version='0.1',
    packages=find_packages(),
    description='A Deep Q-Network (DQN) agent for reinforcement learning tasks',
    author='Cheetah',
)